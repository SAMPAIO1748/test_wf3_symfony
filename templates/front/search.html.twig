{% extends "block.html.twig" %}

{% block main %}

	<h1>Voici les résultats de votre recherche,
		{{ term  }}
		:</h1>
	{% for article in articles %}

		<h2>
			<a href="{{ path('front_post_show', {'id' : article.id } ) }}">{{ article.title }}</a>
		</h2>
		<hr>

	{% endfor %}


{% endblock %}
