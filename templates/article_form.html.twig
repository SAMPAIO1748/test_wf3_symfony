{% extends "block.html.twig" %}

{% block main %}

	{{form_start(articleForm)}}

	{{form_label(articleForm.title, "Titre de l'article", {'label_attr' : {'class' : 'label'} } ) }}
	{{form_widget(articleForm.title, {'attr' : { 'class' : 'input', 'placeholder' : "Titre de l'article" }  } ) }}
	{{ form_errors(articleForm.title)}}

	{{form_label(articleForm.content, "Contenu de l'article", {'label_attr' : {'class' : 'label'} } ) }}
	{{form_widget(articleForm.content, {'attr' : { 'class' : 'input', 'placeholder' : "Contenu de l'article" }  } ) }}

	{{form_widget(articleForm.Enregistrer, {'attr' : { 'class' : 'button' }  } ) }}

	{{form_end(articleForm)}}

{% endblock %}
