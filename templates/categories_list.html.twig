{% extends "block.html.twig" %}


{% block main %}

	{#<pre>
			    {{ dump(categories) }}
			</pre>#}

	{% for category in categories %}
		<h2>{{ category.name }}</h2>
		<p>{{ category.description }}</p>
		<hr>
	{% endfor %}

{% endblock %}
