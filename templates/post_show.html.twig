{% extends "block.html.twig" %}

{% block main %}

	<h1>{{ article.title }}</h1>
	<p>{{ article.content }}</p>

	<h2>Catégorie :
		{{ article.category.name }}</h2>
	<h2>Tag :
		{{ article.tag.name }}</h2>
{% endblock %}
